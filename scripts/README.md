# Scripts

Operational scripts live here.

## Planned scripts
- `generate-token.ts` — create a new QR token record and print its landing URL
- `revoke-token.ts` — revoke an existing token
- `seed-demo-token.ts` — create a local demo token for development
