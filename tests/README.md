# Tests

Mirror `src/` structure where practical.

## Suggested split
- `tests/unit/` for prompt builders, token validation rules, and pure helpers
- `tests/integration/` for route-level behavior like token gating and chat requests
